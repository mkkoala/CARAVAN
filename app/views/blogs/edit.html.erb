<h1>編集画面</h1>
<!-- form_for部分 -->
<%= form_for(@blog) do |f| %>
	<h4>タイトル</h4>
	<%= f.text_field :title %>
	<f4>カテゴリー</f4>
	<%= f.text_field :category %>
	<f4>本文</f4>
	<%= f.text_area :body %>
	<%= f.submit '保存' %>

<% end %>